fastapi==0.136.3
# starlette pinned to the patched release (CVE-2024-47874 / CVE-2025-54121 /
# PYSEC-2026-161); pulled by fastapi but locked here for reproducibility.
starlette==1.3.0
uvicorn[standard]==0.32.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6
pydantic==2.9.2
pydantic-settings==2.6.0
anthropic>=0.97.0
openai>=1.50.0
pypdf>=4.3.0
python-frontmatter==1.1.0
markdown-it-py==3.0.0
celery==5.4.0
redis==5.1.1
httpx==0.27.2
# URL-ingest (issue #6): readability strips nav/chrome from fetched HTML;
# markdownify converts the cleaned HTML to markdown.
readability-lxml==0.8.1
# readability imports lxml.html.clean, which newer lxml (>=5) split into a
# separate package. Without it `from readability import Document` raises
# ImportError and URL HTML ingest silently degrades to raw-chrome HTML
# (caught by fetch_url's fallback). Pin it explicitly. (QA-found 2026-06-12.)
lxml_html_clean>=0.1
markdownify==1.2.2
authlib==1.7.2
PyJWT==2.13.0
python-multipart==0.0.32
# Embeddings are produced via the OpenAI-compatible API (see
# app/services/embeddings.py) using the `openai` client pinned above —
# no local model, so no sentence-transformers/torch/CUDA in this image.
diff-match-patch==20241021

# Tests (artifact smoke suite). pytest is a dev-only dep but lives in
# the main requirements file because the codebase doesn't separate dev
# from prod requirements — keeping with that convention. httpx is
# already pinned above. psycopg[binary] is used by the MCP-publish
# test to insert an api_tokens row directly (works around an unrelated
# pre-existing 500 in POST /api/mcp-tokens).
pytest==8.3.3
psycopg[binary]==3.2.3

# Schema migrations (added 2026-06-12)
alembic>=1.13
